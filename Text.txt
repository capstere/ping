You are Codex running in a strict “no hallucinations” mode.
You MUST only change code based on evidence in the repository and test artifacts.
If you are unsure, add targeted logging/instrumentation and then implement minimal, safe fixes.

CONTEXT (Problem):
We have a PowerShell 5.1 project using OpenXML SDK (DocumentFormat.OpenXml) for writing signature name/date into Excel worksheets.
The signature writing is dynamic: it finds label cells like "Recorded By:" / "Performed By:" / "PQC Reviewed By:" and "Date:" in configured label columns, then writes name/date to a target column (usually adjacent).
Worksheets contain merged ranges for the signature fields (e.g. C54:G54 for name, J54:L54 for date). Excel displays merged values from the owner cell (top-left).
The failure mode:
- If the workbook has previously been “pre-filled” by a colleague’s script (AutoM.ps1) which fills merged tail cells with "N/A", our OpenXML signing succeeds.
- If the workbook is “clean” or someone manually typed in a non-owner/tail cell inside a merge, our signing fails and GUI says “already filled / no cells written” even when the visible cells look blank.
We must be able to overwrite deterministically when Overwrite is enabled, and do not produce false "already filled" messages.
We must NOT rely on EPPlus 6 (we stick to OpenXML + PS5.1). EPPlus 4.5.3.3 exists elsewhere but OpenXML is required here.

GOAL:
Make OpenXML signing 100% robust:
- Merge-aware: always write to merge owner.
- Deterministic overwrite: when Overwrite=true, clear ALL cells in the merge group on the owner row (owner + horizontal tails) and then write owner value; optionally write "N/A" to horizontal tail cells to avoid “ghost values” when user unmerges later.
- “Already filled” logic must check the entire merge group, not just owner, when Overwrite=false.
- Must never crash due to DataType property errors (only operate on DocumentFormat.OpenXml.Spreadsheet.Cell objects, not CellValue/InlineString).
- Must log truthful outcomes: WrittenCells should reflect resolved owner coordinates; sheet should be reported “Written” only if the value is verified.

PROJECT REQUIREMENTS:
- PowerShell 5.1 syntax only.
- No breaking changes to public interfaces unless necessary; if changed, update all call sites.
- Avoid rewriting SharedStringTable: write as InlineString (current design) unless proven otherwise.
- Must keep GDP/Part11 mindset: if overwrite is enabled, we must actually overwrite; if verification fails, raise error.

WHAT TO DO (Steps):
1) Locate the OpenXML signing implementation.
   Find function(s) like Invoke-WorksheetSignature_OpenXml, Set-OpenXmlCellText, Find-FirstRowByContains_OpenXml, Get-MergeCellMap_OpenXml, Verify-WorksheetSignatures_OpenXml.
   Identify how Overwrite is passed from GUI/Main/SignatureHelpers into the OpenXML signer.

2) FIRST: Fix Overwrite propagation deterministically.
   Problem historically: passing -Overwrite:$bool to a [switch] parameter means IsPresent may not behave as intended.
   Choose ONE:
   a) Change OpenXML signer param to [bool]$Overwrite = $false and update all calls accordingly.
   OR
   b) If keeping [switch], change call sites to splat and only include Overwrite when true.
   Implement whichever is safest with minimal changes. Add a unit-like runtime check/log to confirm Overwrite=true is seen inside Invoke.

3) Implement a merge index that provides both:
   - Forward map cellRef -> ownerRef
   - Reverse map ownerRef -> list of all refs in the merge range
   Do NOT break existing Get-MergeCellMap_OpenXml if used elsewhere. Add a new function Get-MergeIndexes_OpenXml returning @{Forward=...; Reverse=...}.

4) Implement a strict cell resolver that ALWAYS returns a Spreadsheet.Cell or $null:
   - Start with Ensure-OpenXmlCell (creates missing cells)
   - But ensure we never end up with CellValue or InlineString objects. If Ensure returns something else, re-resolve using Find-OpenXmlCell and return the actual Cell.
   Add helper: Get-OpenXmlCellSafe($WorksheetPart,$RowIndex,$ColLetter) -> Cell or $null
   Add helper: Clear-OpenXmlCellContent($cell) to null out CellFormula, CellValue, InlineString, DataType.

5) Implement deterministic write function for signing:
   Create function Write-OpenXmlCellText_DeterministicMerge:
   Inputs: WorksheetPart, WorkbookPart, RowIndex, ColLetter, Value, Overwrite(bool), ForwardMap, ReverseMap, TailValue("N/A" or $null)
   Behavior:
   - Resolve targetRef = ColLetter+RowIndex.
   - If ForwardMap contains targetRef, resolve ownerRef.
   - Determine merge group refs: ReverseMap[ownerRef] if exists else just ownerRef.
   - If Overwrite=false:
       - Check existing content across the group (owner + tails on owner row):
         Use Get-OpenXmlCellText + Normalize-OpenXmlText.
         Treat labels ("Recorded By:", "Performed By:", "PQC Reviewed By:", "Date:") as blank.
         If ANY cell in group has non-empty value -> return Written=false.
   - If Overwrite=true:
       - For each ref in group on owner row:
           - $cell = Get-OpenXmlCellSafe
           - Clear-OpenXmlCellContent($cell)
       - Write owner:
           - owner cell InlineString = Value
       - If TailValue is not null/empty:
           - For each tail ref on owner row != ownerRef:
               - Write InlineString TailValue ("N/A")
       - return Written=true and resolved owner row/col.
   MUST NOT throw on DataType because $cell must be Spreadsheet.Cell.

6) Replace existing Set-OpenXmlCellText calls in Invoke-WorksheetSignature_OpenXml:
   - For name and date writes, use Write-OpenXmlCellText_DeterministicMerge with appropriate TailValue.
   - Only mark wroteAny true when that function returns Written=true.

7) Fix WrittenCells logging:
   - Always log the resolved owner coordinates returned by deterministic write function.
   - Verification reads those exact cells.

8) Make status truthful:
   - Add per-sheet verification when Overwrite=true: after writing, call Verify-WorksheetSignatures_OpenXml for that sheet’s WrittenCells.
   - If mismatch -> do NOT claim Written; add Skipped reason "verify mismatch" or throw a clear exception.

9) Extend same merge deterministic model to Seal Test POS/NEG if they are written via OpenXML:
   - If Seal Test is EPPlus-only and already stable, do not change.
   - If there is OpenXML writing to merged cells for Seal Test, use the same deterministic merge write helper.

10) Provide deliverables:
   - A unified diff patch with changes.
   - Updated zip.
   - Short README snippet explaining the new deterministic merge overwrite behavior and how Overwrite is propagated.
   - Add a debug switch or optional logging that can be enabled to output per-sheet: found nameRow/dateRow, resolved ownerRef, written status.

TESTING YOU MUST DO:
- Use provided example workbooks:
  Case A: workbook where colleague prefilled N/A tail cells -> signing should still work.
  Case B: clean workbook with merged ranges but empty visible cells -> signing should work.
  Case C: manual entry in a tail cell inside a merge (e.g., E54 in C54:G54), with Overwrite=false -> should refuse (already filled).
  Case D: same but Overwrite=true -> should overwrite and tail becomes N/A.
- Confirm no DataType property errors.
- Confirm GUI status matches reality: WrittenCells not empty when sign succeeded.

DO NOT:
- Do not change business logic unrelated to signing.
- Do not introduce EPPlus 6.
- Do not remove existing functions unless replaced safely.
- Do not “assume” merge ranges are horizontal; filter tail writes to ownerRow only.

Output:
- Provide the diff patch.
- Provide the updated OpenXmlHelpers.ps1 and callsite changes (Main/SignatureHelpers) needed to propagate Overwrite.
- Provide a small debug log example in comments.

Repository + files are attached. Work only from them.
